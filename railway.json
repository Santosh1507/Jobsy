{
  "$schema": "https://railway.app/schema.json",
  "name": "jobsy",
  "builder": "DOCKERFILE",
  "run": {
    "command": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "environment": [
    "PYTHONUNBUFFERED=1"
  ]
}